[profile.default]
# Limit test parallelism to avoid PostgreSQL connection exhaustion
test-threads = 4

# Only show failures, not all test output
status-level = "fail"

# Show test output only on failure (not for passing tests)
failure-output = "immediate-final"
# success-output = "never"
